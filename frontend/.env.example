# Base URL of the deployed backend API, including the /api prefix.
# Local dev: leave unset, vite.config.ts proxies /api to http://localhost:3000.
# Production (Namecheap): set this at build time before `pnpm build`.
VITE_API_URL=https://api.sturdylifer.com/api
